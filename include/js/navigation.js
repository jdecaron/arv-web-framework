function userLinkClick(url, type){

    // Process the URL with the AJAX
    // page loading system.
    if(type == 'ajax'){
        // 
        url_array = url.split('&')
        alert(url_array[0]);
    }
}
